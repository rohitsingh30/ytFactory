You are ONE of several parallel agents de-slopping the ytFactory repo (an
AI-generated automated YouTube video pipeline). It is full of AI slop: docstrings
and comments that drifted from the code, references to deleted modules, dead code,
aspirational comments. RULE: the function BODY is authoritative — never trust a
comment/docstring over the code.

VERIFIED architecture (rely on this — confirmed via telemetry + code):
- 7-stage worker: rewrite → cast → images → tts → asr → compose → upload.
- rewrite/cast/prompts are LLM stages in pipeline/llm/**; images are generated by
  pipeline/images/** calling the Z-Image-Turbo Cloud Run service.
- LLM stages run through a constraint-aware orchestrator that validates output
  inline and retries with a focused diff prompt.

YOUR SCOPE — edit ONLY `pipeline/llm/**` and `pipeline/images/**`. Nothing else.
Slop elsewhere → one line in de-slop/HANDOFF.md.

KNOWN SLOP IN YOUR SCOPE (verify against code, then fix/delete):
- pipeline/images/prompt_refiner.py: comments must describe the CURRENT behavior,
  where a whole-batch or every-beat refiner failure RAISES RuntimeError
  ("silent-fallback-unshippable-output" gate). Delete any older comment implying
  the refiner silently falls back and the render continues — that is stale.
- pipeline/images/images.py: the `prompts.legacy_path` vs refined-prompt branch —
  make comments match which path the code actually takes.
- pipeline/llm/orchestrator.py + contracts/*: retry/validation docstrings should
  match the real retry cap + failure severities in the code.
- Verify NO references to deleted models/providers or to
  `pipeline.render.{shorts,long_form}`.

FIX (de-slop only — no refactor, no features):
1) Comments/docstrings contradicting code → fix or delete.
2) References to deleted modules/providers → grep-confirm, fix or remove.
3) Dead code: private symbol with 0 callers or file with 0 importers → remove.
   BEFORE removing: `grep -rn "<name>" .` repo-wide; if used outside your scope →
   log to HANDOFF.md instead.
4) Always-on/off flags → collapse to real behavior.

GUARDRAILS: no behavior change; don't rename public symbols used outside your scope
(grep first); surgical edits only.
CAUTION: "legacy" is NOT always slop — some legacy names are LIVE compat code. Do
NOT remove pipeline/llm/rewrite.py's YTFACTORY_REWRITE_USE_LEGACY flag or
script_schema.to_legacy_dict(); they are real. Only delete provably-dead references.

VERIFY before finishing:
- `.venv/bin/python -c "import pipeline.llm.orchestrator, pipeline.images.images, pipeline.images.prompt_refiner"`
- `.venv/bin/pytest tests/ -q -k "llm or refiner or prompt or cast or rewrite or image or orchestr"`
- Revert anything that goes red. Leave edits staged, uncommitted.

OUTPUT: bullets — files cleaned; dead code removed (+ proof of 0 callers);
HANDOFF.md notes.
