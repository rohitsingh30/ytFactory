# /make-sleep-history — learnings index

Append a one-line note per regression after every `/critique-audio`
or `/critique-video` run. Class-of-bug fixes go to `pipeline/<x>.py`
or `historyrecapped/learnings/<topic>.md`; one-offs stay here.

Format: `YYYY-MM-DD — <slug> — <one-line>` followed by `→ <fix path>`
on the next line.

## Regressions

<!-- empty — first run TBD -->

## Class-of-bug escalations (linked from this skill's quality gates)

<!-- list every gate that exists BECAUSE a regression fired twice -->
