# /make-katha — learnings index

Empty on day 1. The skill's self-learning loop (SKILL.md §8)
appends here as regressions surface from `/critique-audio` /
`/critique-video` runs against rendered kathaa videos.

Format: each entry is a Markdown file in this dir; this index
holds one-line pointers (≤150 chars).

Class-of-bug fixes mirror to `hindutavaanimated/learnings/<topic>.md`
per CLAUDE.md dual-save rule.

## Entries

<!-- pointers go here, one per line -->
