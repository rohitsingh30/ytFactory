# /tune-ai-extraction — learnings index

Newest entries on top. Each line points at a per-patch JSON-lines row
in `scorecard.jsonl` and (for class-of-bug fixes) a topic doc.

Empty on day 1.

---

## Class-of-bug fixes (each has a `<topic>.md` and `docs/<topic>.md`)

(none yet)

## Per-patch scorecard (jsonl rows in `scorecard.jsonl`)

(none yet — first run will append rows here as one-liners)

## Missed classes (gaps the audit didn't flag, surfaced by /critique-*)

See `missed_classes.md` (created on first miss).
