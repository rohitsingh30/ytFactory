---
job_id: 845bdb0df20e4ba3885ca33c7749e74d
channel: mystoriesanimated
variant: null   # Firestore variant=None; niche=r/nosleep (long-form)
slug: i-ve-been-flying-for-almost-thirty-hours-and-the-flight-attendants-won-t-stop-cr-845bdb0d
state: done
diagnosed_at: 2026-05-24T00:00:00Z
findings_count: 12
new_fragility_candidates: 3
new_o_candidates: 6
---

# Render diagnosis — 845bdb0df20e4ba3885ca33c7749e74d

## Job summary

- channel/variant: `mystoriesanimated` / `null` (Firestore `variant` is null;
  the render is the r/nosleep long-form niche, NOT a real variant overlay —
  no `pipeline/variants/mystoriesanimated/nosleep.yaml` exists)
- slug: `i-ve-been-flying-for-almost-thirty-hours-...-845bdb0d`
- topic: "I've Been Flying for almost Thirty Hours and The Flight Attendants Won't Stop Crying"
- final state: `done` (Firestore — `status`/`stage`/`final_mp4` fields are
  all empty; only `timeline[]` reports `done`; see Finding 4)
- worker execution: `ytfactory-render-worker-v2-vrftg`
- worker image SHA: `cc0314d2df4ca8b9ad6861577defb55d52b3df754c8805d1d14809e39cb7ede5`
- wall-clock: 55m 04s (`2026-05-23T21:25:13Z` → `2026-05-23T22:20:21Z`)
- compose output: `gs://ytfactory-prod-v3-artifacts/jobs/845bdb0df20e4ba3885ca33c7749e74d/short.mp4`
  (76.78 MB; `compose.run` event @ `22:20:12.916Z`, `meta.output_bytes=76783486`)
- failure stage: n/a — render reached `compose` + `upload` successfully;
  output mp4 is visibly unshippable (see Findings 1, 2, 3, 6).

## Stage timeline

**Caveat (Finding 4):** the only Cloud Logging `stage.start` / `stage.end`
emitted for this job covers the `upload` stage. The rest of the timeline
below is reconstructed from per-event timestamps + Firestore `timeline[]`
entries used as a proxy. Tagged as F25-class telemetry gap.

| stage | started (event ts) | ended (event ts) | duration | source | notes |
|---|---|---|---|---|---|
| rewrite (outline + sections) | `21:25:13Z` (job create) | `21:28:12.139Z` (last `llm.call`) | 164.7s (Firestore) | `llm.call`×17 + Firestore `timeline[rewrite]` | 1 outline + 16 section bodies; **1 `llm.retry` fired** on outline (attempt 2, `finish_reason=length`, `reason=max_tokens_double`, retry succeeded — meta @ `21:26:25.119Z`). Body capture present on all 17 `llm.call` (`input_preview` len=4000 chars). |
| cast | — | — | — | Firestore `timeline[cast]` `msg="skipped — long-form has no cast stage"` | No events; long-form path bypasses cast. |
| engine.pick | `21:28:13.139Z` | — | — | event | `meta.chosen=render_long`, `meta.kind=long_form`. |
| tts | `21:42:38.263Z` | `21:42:57.277Z` (last `tts.chunk` emit) | 884.8s wall (Firestore) | `tts.chunk`×99 (success=99/0) | Provider `cloudrun_chatterbox`, voice `/workspace/pipeline/voice_refs/tifo_devine.wav`. Body capture present on all 99 chunks (`input_preview` len ≥ 104 chars). NB: emit timestamps are clustered in 19s; the 884.8s Firestore figure includes the upstream HTTP wait. |
| asr | — | — | — | Firestore `timeline[asr]` `msg="skipped — captions aligned from authored TTS chunk timings"` | No `asr.*` events at all. ASR step bypassed; captions derive from TTS-side timings. |
| images | `21:48:21.560Z` (first `image.gen`) | `22:06:39.745Z` (last `image.gen`) | 1776.9s wall (Firestore); `sum(duration_ms)=8279s` = parallel fan-out | `image.gen`×77 (success=77/0), `image_gen`×77 (legacy duplicate), 1 `image.gen.retry` (panel rate-limited 503, recovered on attempt 2) | All cache_hit=False. Body capture present on `image.gen` (77/77 `input_preview`) but absent on the duplicate `image_gen` event class (see Finding 5). Per-panel mean 107.5s, median 93.8s, max 291.4s (13/77 panels > 180s — yellow flag per SKILL.md). |
| timeline | `21:46:17.543Z` (`timeline.beat_map`) | — | — | event | `anchor_method=local_anchors`, `beat_count=11`, `total_seconds=1612.347`, `unanchored_count=0`. |
| music | `22:15:55.068Z` (`music.pick`) | — | — | event | `source=missing_bed`, `track_id=silent` — see Finding 6. |
| overlay | `22:15:57.266Z` → `22:16:09.772Z` | 13s | 11 `overlay.render` events all `kind=word` | event | 11 word-overlay PNGs for ~1612s of audio = one PNG per 146.5s — see Finding 3. |
| compose | (compose.run aggregated) | `22:20:12.916Z` | `meta.duration_ms=242785` (242.8s) + 2236.7s Firestore wall | `compose.run` + `ffmpeg.call`×82 (80 unspecified-purpose ffmpegs + 1 `section_video_mux` + 1 `silent_music`) | Output `/workspace/data/mystoriesanimated/long_form/...mp4`, 76.78 MB. |
| upload | `22:20:13.916Z` (`stage.start`) | `22:20:14.916Z` (`stage.end`) | 339 ms (event) + 0.4s (Firestore) | event | The ONLY stage with a real envelope — see Finding 4. |

## Findings (chronological)

### 1. Anti-text safety prefix dominates 56% of every image prompt and resolves Z-Image-Turbo to "blank jersey product photo"

- **Stage:** images.
- **Evidence (events + artifacts):**
  - All 77 `/tmp/image_meta_845bdb0d/*.json` start verbatim with the 189-char
    string `"Clean surface, unmarked, blank jersey, smooth fabric, plain
    backgrounds, unmarked book covers, unlabeled bottles, no signage, no
    banners, no watermark, no logo, no caption, no street signs."`
    — confirmed 77/77.
  - Source: `pipeline/images/images_cloudrun.py:492-499` defines this as
    `ANTI_TEXT_PREFIX`; `_append_anti_text_suffix` (despite the name,
    it PREPENDS) attaches it to every cloud-Z-Image-Turbo call.
  - Stats over the 77 panels:
    - Longest common prefix across **all 77 panels**: **190 chars**.
    - **Mean boilerplate / total_chars ratio: 0.626** (target ≤0.30).
    - **Max ratio: 0.818** (`image_meta/00076.json`, total 231 chars).
    - **Min boilerplate-bearing ratio: 0.311**.
    - Mean pairwise identical-prefix ratio (sampled): **0.512**.
    - Verb-led "scene" clause begins at **char 190 of every prompt**
      (target: chars 0-150 per memory `project_z_image_turbo_verb_led_prompts.md`).
  - Visual proof: `/tmp/panels_845bdb0d/panel_076.png` is a floating
    white t-shirt on grey backdrop with no human and no scene — the
    final_prompt is the 189-char boilerplate + 41 chars of scene
    ("Wide quiet room scene with narrator alone reflecting") and the
    model resolved the dominant tokens "blank jersey, smooth fabric,
    plain backgrounds" as the subject.
  - 58/77 panels (75%) have **no human subject token** in their prompt
    at all (panels 2,4,6,7,9,10,12,13,15,16,18,…,76) — environment-only
    scenes that the boilerplate hijacks.
- **Fragility match:**
  - F11 (`pipeline/render/visualize/_fallback.py`): silent-fallback class
    of visible-broken artifact; this is a non-fallback version of the
    same symptom — the boilerplate path *succeeds* but produces an
    artifact equivalent to silent fallback.
  - F12 (ai_beat_slideshow silent-fallback) — same class.
  - F20 (`_PER_BEAT_FAILURE_THRESHOLD=0.10` first-failure kill, no
    per-beat retry-with-stronger-prompt) — and no post-gen image-quality
    validator means 75% boilerplate-dominant prompts ship as panels.
  - Memory `project_z_image_turbo_verb_led_prompts.md` rule 13 violated
    (verb-led key_visual buried at char 190+, not chars 0-150).
  - Memory `project_z_image_turbo_verb_led_prompts.md` rule 16 violated
    ("no X / no Y" negations stuffed into POSITIVE prompt; the comment
    at `images_cloudrun.py:480-490` claims this is positive framing
    but "blank jersey, unmarked book covers, no signage" are negations
    in surface clothing — Z-Image-Turbo treats the noun "blank jersey"
    as the SUBJECT, exactly the failure the memory predicts).
- **Fix target:**
  - O8 (Z-Image-Turbo refiner rewrite — output 100-200-word structured
    positive-only prompt; verb-led first; channel style anchor in
    chars 0-150).
  - O9 (per-beat retry with image-quality validator — would catch the
    floating-jersey panels post-gen and retry with stronger prompt).
- **Severity:** HIGH — every shipped frame is degraded; this is the
  visible failure the critique flagged.

### 2. Zero style anchor in any prompt — channel `image_style_prefix` never reaches the renderer

- **Stage:** images (upstream wiring bug).
- **Evidence:**
  - `0/77` panels contain ANY of: `photoreal | illustrated | animated |
    cinematic | cartoon | realistic | studio | stock photo | film |
    illustration | 3d render | anime | painted | sketch | comic`.
  - `pipeline/channels/mystoriesanimated.yaml` declares TWO style
    prefixes: top-level (warm hand-drawn watercolor) at L194-198, AND
    `long_form.image_style_prefix` ("Flat 2D crayon-style children's
    drawing…") at the same file's `long_form:` block.
  - The outline-LLM `input_preview` (events_log @ `21:27:25Z`) shows
    the LLM was given `"Visual aesthetic: Flat 2D crayon-style
    children's drawing with thick black outlines and soft pastel fill
    colors"` — so the long_form override REACHED the LLM channel-context
    prompt.
  - But none of those 77 `image_meta` `final_prompt` strings contain
    "crayon", "watercolor", "ink line", or any other style token —
    the renderer applied **neither** style prefix to the actual
    image-gen call.
  - Visual proof: `/tmp/panels_845bdb0d/panel_000.png` and
    `/tmp/panels_845bdb0d/panel_038.png` are **photoreal** stills of
    a woman in a yellow tee, not crayon, not watercolor — the
    "style-less" prompts default to Z-Image-Turbo's photoreal mode.
- **Fragility match:** [NEW-FRAGILITY-CANDIDATE] **F27** —
  *channel `image_style_prefix` is lossy through engine → visualize
  plugin → images_cloudrun call. Long-form path reads the LLM
  channel-context but never prepends the style prefix to the per-panel
  Z-Image-Turbo wire prompt.* The result is the channel's aesthetic
  brand silently absent across 100% of long-form renders. Same class
  as F22 (caption_style lossy through overlay → compose) — wiring
  drops a documented spec field between the YAML and the wire payload.
- **Fix target:** [NEW-O-CANDIDATE] **O33** — wire
  `image_style_prefix` end-to-end. Plugin reads
  `spec.extra["image_style_prefix"]` and prepends it to the per-panel
  prompt before `_append_anti_text_suffix` runs. Add a regression
  test pinning that the renderer's wire prompt contains the channel's
  style_prefix substring. Sibling to F22/O13's caption wire-up shape.
- **Severity:** HIGH — explains the "4+ incompatible visual styles"
  the previous shallow pass noted; in reality it's **one** style
  (photoreal default) on most panels and **one** style (floating
  product photo) on panels where the boilerplate dominates.

### 3. Word-caption coverage is one PNG per 146s of audio (11 PNGs for 1612s of narration)

- **Stage:** overlay → compose.
- **Evidence:**
  - 11 `overlay.render` events @ `22:15:57.266Z` → `22:16:09.772Z`,
    each `meta.kind="word"`, `meta.count=1`, `meta.total_chars` in
    [2351, 3236].
  - `compose.run` `meta.inputs` contains exactly 11 entries matching
    `/tmp/render/.../word_captions/word_0000.png` … `word_0010.png`.
  - Narration duration per `music.pick` event: 1612.347s.
  - 1612s / 11 PNGs = **one caption PNG per 146.6s of audio** — these
    are PER-SECTION caption strips, not per-word.
  - 0 `*.ass` (sentence-caption ASS) entries in `compose.run` inputs.
- **Pipeline-side root cause:** `pipeline/render/overlays/word_caption_pngs.py`
  appears to be emitting one PNG per section (or per `total_chars`
  ~2000-3000-char block) rather than per spoken word. The compose
  layer then renders each PNG as a static overlay for the duration of
  the section — i.e. a horizontal strip of words held for 2.5 min.
- **Fragility match:** F22 (caption wiring lossy end-to-end).
- **Fix target:** O13 (wire `spec.caption_style` properly; add post-render
  caption-density gate); O28 (post-render caption density gate
  computed against audio time; would have hard-failed this render
  since text density is implausibly low for 1612s of audio).
- **Severity:** HIGH — captions are visibly broken in the final mp4
  (per the critique cross-reference); also a Q9 reliability pain
  item (renders "succeed" but ship with wrong captions).

### 4. Stage-envelope coverage is 1/8 stages — only `upload` wraps

- **Stage:** cross-cutting (telemetry).
- **Evidence:**
  - Of 371 events emitted for this job, exactly **1 `stage.start`** and
    **1 `stage.end`** fire, both for `meta.stage=upload`
    (`22:20:13.916Z` / `22:20:14.916Z`, dur 339ms). Zero envelopes for
    rewrite / cast / engine.pick / tts / asr / images / timeline /
    overlay / compose.
  - Firestore `decision_log[]` is **empty** (0 entries) despite the
    bigbang adding `decision_log` writes; the artifact subdir
    `gs://…/decision_log/` exists but is empty (only the directory
    marker showed up in `gsutil ls`).
  - The Firestore `timeline[]` array has 7 stage entries with
    `status: done` and `msg` durations, but these are
    application-side stage marks, NOT Cloud-Logging envelope events.
- **Fragility match:** F25 — "rev-healthy = deploy complete" gap.
  The render-worker rolled out the stage-envelope feature (`_stage_envelope.py`
  is COPY'd per fix `8345624`) but the envelope wrapping is only
  applied around the `upload` stage in the actual worker code path
  for this render-kind. Either:
  - the upstream stages were never wrapped, OR
  - the wrapping is in a code path the long-form engine bypasses.
- **Fix target:** O30 (preflight render in every `deploy.sh` post-rollout
  — would have caught "only 1 of N stages emit envelopes"). PLUS
  **[NEW-O-CANDIDATE] O34** — *bring stage-envelope coverage to all 8
  pipeline stages on render-worker-v2.* Today the wrapping exists on
  upload only; rewrite/tts/asr/images/timeline/overlay/compose need
  the same `_stage_envelope.wrap_stage()` decoration with their
  meta.stage tag. Plus a counter-test in
  `tests/observability/test_stage_envelope_coverage.py` that
  enumerates the stages a long-form render must emit and fails if any
  are absent.
- **Severity:** MEDIUM — the render itself shipped, but every future
  `/diagnose-render` invocation is degraded because we can't see
  per-stage duration / success from events alone (we fall back to
  Firestore `timeline[]`, which is reliable but lower-resolution).

### 5. Duplicate `image.gen` + `image_gen` event class — body capture absent on the legacy duplicate

- **Stage:** images (telemetry shape).
- **Evidence:**
  - 77 `image.gen` events AND 77 `image_gen` events fire — same panels,
    same panel_index, same seeds, same durations to the ms (`image.gen`
    panel_76 dur=238413ms; `image_gen` panel_76 dur=238412ms).
  - `image.gen` (dotted) carries body capture: `meta.input_preview`
    populated 77/77, `meta.final_prompt_preview` populated, `cfg=0`,
    `final_prompt_sha256` present.
  - `image_gen` (underscore — legacy) carries `out_path`, `out_bytes`,
    `prompt_chars`, `style_chars=0`, `provider=cloudrun_z_image_turbo`
    — but no `input_preview`, no `final_prompt` body. Same TTL.
- **Fragility match:** [NEW-FRAGILITY-CANDIDATE] **F28** —
  *image-gen telemetry is emitted from two code sites: the canonical
  `track_event("image.gen", …)` AND a legacy `track_event("image_gen", …)`
  shadow site. Both fire per panel, doubling Cloud Logging spend and
  Cloud Logging cardinality on `event` names; downstream consumers
  must dedupe by panel_index + duration or count panels at 2× truth.*
  Sibling to F2 (untyped spec.extra) — same root cause class
  (parallel-naming drift across code paths).
- **Fix target:** [NEW-O-CANDIDATE] **O35** — *delete the legacy
  `image_gen` (underscore) emit site; consolidate on `image.gen`
  (dotted).* Add a counter-test in
  `tests/observability/test_no_duplicate_event_names.py` that
  asserts no two emit sites in `pipeline/` produce events that differ
  only by `.` vs `_`. Effort S (single grep + delete).
- **Severity:** LOW (cosmetic — wastes Cloud Logging budget; could
  mislead a future diagnose-render run that double-counts panels).

### 6. Music bed missing — silent track used despite channel YAML declaring `music_bed_default: ambient_low.mp3`

- **Stage:** music.
- **Evidence:**
  - `music.pick` @ `22:15:55.068Z`: `meta.source=missing_bed`,
    `meta.track_id=silent`, `meta.mood=none`, `meta.duration_s=1612.347`.
  - `pipeline/render/music/ducked_loop.py:96-112` shows the fallback
    fires when `bed_path is None or not bed_path.exists()` —
    `_logger.warning("ducked_loop: bed %r not found at %s — falling
    back to silent", …)`.
  - Channel YAML declares `music_bed_default: ambient_low.mp3` in
    BOTH the top-level `audio:` and `long_form:` blocks. The asset
    wasn't found at the resolved path inside the worker container.
- **Fragility match:**
  - F24 (Dockerfile-COPY drift) — *applied to the music_assets/
    directory class.* The Dockerfile uses explicit per-file COPY and
    music-bed mp3s are likely not in the COPY graph.
  - Sibling pattern to the `_tel_track_io.py` silent-fail described
    in memory `project_dockerfile_copy_drift.md`: silent-mode fallback
    + revisions go healthy + the operator sees `track_event` fire
    happily reporting "music.pick success=True" but `source=missing_bed`.
- **Fix target:**
  - O30 (preflight render that verifies music_bed exists and is
    non-silent before declaring deploy complete).
  - [NEW-O-CANDIDATE] **O36** — *audit the worker image's
    `pipeline/music/` (or wherever `music_bed_default` resolves to)
    and either COPY the mp3 set in `cloud/render-worker-v2/Dockerfile`
    or pull from GCS at runtime. Hard-fail (not silent fallback) when
    a configured `music_bed_default` is missing — same shape as the
    fail-loud audit 2026-05-15.* Effort S.
- **Severity:** MEDIUM — the render survived but ships without the
  ambient bed the channel YAML mandates; viewer hears dead silence
  under narration, breaking the audio-mix contract.

### 7. Outline LLM padded panel-scene fields with the channel character_description verbatim (no refiner stage in between)

- **Stage:** rewrite → images.
- **Evidence:**
  - `events_log/events.jsonl` `llm.module.rewrite_long_form_outline`
    `output_preview` shows the outline LLM emitted `panel_briefs[*].scene`
    fields like *"A 29-year-old woman with shoulder-length wavy brown
    hair rendered in visible strand groups, almond-shaped eyes with
    dark pupils and short eyelashes, expressive eyebrows, small
    defined nose with faint shadow on the right side, soft pink blush
    on cheekbones, wearing a fitted mustard-yellow cotton t-shirt and
    faded blue jeans, seated by an airplane window during boarding
    with passengers placing bags in overhead bins."*
  - This 324-char prefix exactly matches the channel YAML's
    `character_description` field at
    `pipeline/channels/mystoriesanimated.yaml:97-118`.
  - 19/77 panels contain this verbatim character spec; 58/77 panels
    have NO human subject (the LLM correctly omitted the spec for
    landscape/environment shots).
  - The outline-LLM was instructed by
    `pipeline/llm/rewrite_long_form.py:220-232` to "*repeat the
    WHOLE character spec verbatim*" in every panel — and 19 of those
    77 panels obey.
  - **There is no refiner stage** in this render: no
    `gs://…/prompts_refined/` artifact, no `gs://…/refiner_io/`
    artifact, zero `image.refiner.batch` / `image.refiner.fallback`
    events. The long-form path skips the refiner that
    `pipeline/images/prompt_refiner.py` provides.
- **Fragility match:**
  - F2 (untyped `spec.extra`) — the character_description gets
    embedded twice: once in the LLM prompt context, once in the
    panel-scene string, and finally a third time via images_cloudrun's
    anti-text prefix — three independent prompt-assembly layers with
    no orchestrator.
  - F11/F12 — silent quality degradation; the render succeeds even
    though the prompt-assembly path collapses to product-photo mode.
  - [NEW-FRAGILITY-CANDIDATE] **F29** — *long-form image path has
    no refiner stage. The outline-LLM authors panel-scene strings
    directly and `pipeline/images/prompt_refiner.py` is bypassed.*
    Result: O8 (refiner rewrite to verb-led, 100-200-word, positive-only,
    style-anchored) does NOT lift long-form even if implemented —
    long-form needs its OWN refiner gate or O8 must wire the refiner
    into the long-form path.
- **Fix target:**
  - O8 (refiner rewrite) — but must be EXTENDED to wire into the
    long-form path, not just the short path.
  - [NEW-O-CANDIDATE] **O37** — *wire `prompt_refiner` into the
    long-form image path.* Today `pipeline/render/visualize/longform_panels.py`
    builds `image_panels_video` from raw `panel.scene` strings. Add
    a per-panel refiner pass that takes `(panel.scene, channel.style_prefix,
    cast.character_description)` and emits a verb-led 100-200-word
    structured prompt. Effort M. This is the load-bearing fix that
    closes Finding 1.
- **Severity:** HIGH — root cause of Finding 1.

### 8. `image_steps=4` shipped despite channel YAML declaring `image_steps: 9` in BOTH top-level and long_form blocks

- **Stage:** images (spec_enrich wiring bug).
- **Evidence:**
  - 77/77 `image_meta/*.json` have `"steps": 4`.
  - `pipeline/channels/mystoriesanimated.yaml` declares `image_steps: 9`
    at the top-level (L102) AND at `long_form.image_steps: 9` (L191).
  - `pipeline/render/visualize/longform_panels.py:96` reads
    `int((spec.extra or {}).get("image_steps", 4))` — falls back to
    4 when spec.extra has no key. So `spec.extra["image_steps"]` is
    not being populated from either YAML location.
  - `_clamp_steps` in `pipeline/images/images_cloudrun.py:546-557`
    accepts 4 within `[lo=4, hi=12]` — so the value passes through
    cleanly.
- **Fragility match:**
  - F2 (untyped spec.extra).
  - F16 (spec_enrich ordering invariant — `populate_render_extras`
    must precede plugin reads; this is a "missing populator" sibling
    case where there's no populator at all).
  - F19 in cousin form — channel YAML keys that are not wired into
    `spec_enrich.populate_render_extras()` silently default.
- **Fix target:**
  - O25 (audit-able RenderSpec.extra schema; would have caught the
    missing image_steps populator at module-load).
  - O26 (make `populate_render_extras` non-optional via
    engine-internal call).
- **Severity:** MEDIUM — every long-form render under-uses Z-Image-Turbo
  (4 steps vs the documented 9-step sweet spot at
  `cloud/image-z-image-turbo/server.py:149`). Lifts on every future
  long-form mystoriesanimated render, plus the 5 sibling variants
  (`aita_*.yaml`, `tifu.yaml`, `wiki_oddities.yaml`,
  `today_in_history.yaml`) — all declare `image_steps: 9` but spec_enrich
  isn't reading variant-level overlays either.

### 9. `image_steps=9` documented sweet-spot ignored → 13/77 panels still take >180s each

- **Stage:** images (cost/quality).
- **Evidence:**
  - `image.gen` durations: min=29.6s, median=93.8s, mean=107.5s,
    max=291.4s; 13/77 panels exceed 180s (yellow flag per SKILL.md).
  - `cloudrun_z_image_turbo` `max-instances=4` (per cost-rules; see
    CLAUDE.md) — so 77 panels fan out through 4 GPUs in ~21-min
    wall-clock = 8279 GPU-seconds / 4 instances ≈ 2070s wall ≈ 34.5
    min. Matches the Firestore `timeline[images].msg=1776.9s` figure
    well enough that the fan-out is healthy.
  - But the >180s tail (13 panels) suggests Z-Image-Turbo cold-start
    or post-Cloud-Run-restart on individual instances; not a
    quality issue per se, but a cost-tail.
- **Fragility match:** none direct — operational signal.
- **Fix target:** Operational tuning (out of scope for /diagnose-render);
  potentially track in O20 (per-render OTel trace) to surface the
  per-panel cold-start tail.
- **Severity:** LOW.

### 10. Outline LLM `llm.retry` fired on attempt 1 (`finish_reason=length, max_tokens_first=4096`)

- **Stage:** rewrite (outline).
- **Evidence:** `llm.retry` event @ `21:26:25.119Z`: `stage=rewrite_long_form_outline`,
  `attempt=2`, `reason=max_tokens_double`, `max_tokens_first=4096 →
  max_tokens_retry=8192`, `finish_reason=length`, `reasoning_effort=minimal`.
  The retry succeeded at `21:27:25.127Z` (114179ms dur on the successful
  `llm.call`).
- **Fragility match:**
  - F3 + F18 (long-form parallel fan-out — outline came too close to
    max_tokens; locked at `minimal` reasoning_effort per F-class).
  - Memory: this is the documented failure mode in `O4` — flip
    `reasoning_effort` to `medium` for rewrite_long_form. Note:
    O4 was IN-FLIGHT in W1 refactor work per the in-flight markers
    in `/ai/improvement-opportunities.md` (line 24 status block).
- **Fix target:**
  - O4 (flip reasoning_effort to medium for rewrite_long_form). The
    outline retry happens to be cheap on this render (62s burned
    on attempt 1) but compounds across renders.
  - O1 + O3 (word_count emission + outline sum-check) — would have
    caught this at outline-time, not section-body-time.
- **Severity:** LOW for this render (retry recovered cleanly); MEDIUM
  for the channel (every long-form is one outline retry from a
  multi-min cost spike).

### 11. Channel-rule compliance: closer pattern mismatch (channel YAML targets AITA, render is r/nosleep)

- **Stage:** cross-cutting (channel-rule check).
- **Evidence:**
  - `pipeline/channels/mystoriesanimated.yaml:128` declares
    `closer_format: "LIKE if YTA, COMMENT if NTA. AITA?"` — that's an
    **AITA Shorts** closer.
  - This render is long-form r/nosleep, narration ends at:
    *"...The reports say the situation was under control the entire
    time. Maybe that's true. But I still wonder how close we came to
    a version of that flight where the story ended very differently."*
  - The narration does NOT use the channel YAML's configured closer.
    Long-form path may correctly ignore it (AITA closer is wrong for
    nosleep), but no `long_form.closer_format` override exists in the
    YAML's `long_form:` block.
  - Duration: 1612s narration vs `long_form.duration_target_s: [600, 1800]`
    = within band (96.7% of upper bound).
  - Narration WPM check: top-level narration ≈ 29785 chars ÷ 1612s
    × 60 = 1108 chars/min ≈ 175 WPM at 5 chars/word — slightly fast
    for a sleep-channel narration; matches `tts_speed: 0.95 ×
    tts_post_atempo: 0.92` = effective 0.874 speedup applied to a
    natural ~200 WPM Chatterbox baseline = ~175 WPM. Plausibly in-band.
- **Fragility match:**
  - F6 (closer-panel dead code in compose) — sibling pattern (closer
    config exists, may or may not render).
  - Channel-rule: the AITA closer is incompatible with r/nosleep niche
    and there's no per-niche closer override.
- **Fix target:**
  - O12 (port closer panel to overlay plugin slot) — would let the
    long-form path declare its own closer.
  - Or simpler: add `long_form.closer_format: null` to the channel
    YAML and a per-niche closer in the long_form rewrite prompt.
- **Severity:** LOW — narration's natural ending is acceptable; no
  visible closer regression in the mp4.

### 12. `decision_log[]` empty + envelope/ artifact misnamed

- **Stage:** cross-cutting (telemetry).
- **Evidence:**
  - Firestore `jobs/845bdb…` doc: `decision_log[]` = 0 entries.
  - `gs://…/decision_log/` directory exists but is empty.
  - `gs://…/envelope/i-ve-been-flying-…-845bdb0d.envelope.json`
    exists but is just the original `script.json` payload (slug,
    kind, title_options, source_url, source, long_form blob) —
    NOT stage envelopes. The "envelope" subdir name is misleading;
    it's the script artifact, not a stage-envelope artifact.
- **Fragility match:**
  - F25 (rev-healthy = deploy complete gap) — telemetry surface is
    incomplete; the "decision_log" feature was rolled out as a
    Firestore field + a GCS subdir, but no writes are firing.
  - Sibling to Finding 4 (stage-envelope coverage 1/8).
- **Fix target:**
  - [NEW-O-CANDIDATE] **O38** — *audit + restore decision_log writes.*
    Every render should emit at minimum: (1) `engine.pick` decision
    (which engine + why); (2) `music.pick` decision (which bed + why
    fallback fired if it did); (3) `image.gen.retry` per-panel
    retry decision. Today only `engine.pick` and `music.pick` fire as
    EVENTS — neither lands in Firestore `decision_log[]` nor in the
    GCS `decision_log/` artifact. Effort S (wire `firestore_helpers.decision_log_append`
    into the three sites). Also rename the misleading `envelope/`
    subdir to `script/` (the script.json already exists under
    `script/`, so `envelope/` is a duplicate write).
- **Severity:** MEDIUM — every future `/diagnose-render` invocation
  loses the "human-readable narrative of we-chose-X-over-Y" channel
  that the SKILL.md playbook expects.

## Cross-cutting observations

### Prompt monotony

- **77 panels analysed.** Longest common prefix across ALL 77 panels:
  **190 chars** (the `ANTI_TEXT_PREFIX`).
- Common prefix as fraction of mean prompt length: **0.563** (56.3%).
- Max single-panel boilerplate ratio: **0.818** (`image_meta/00076.json`,
  the panel rendered as a floating white t-shirt).
- Mean pairwise identical-prefix ratio (20-panel sample): **0.512**.
- **Verb-led key_visual position: char 190 of every prompt** (target:
  chars 0-150 per memory `project_z_image_turbo_verb_led_prompts.md`).
  The "scene clause" begins exactly at the 190-char `ANTI_TEXT_PREFIX`
  boundary on every panel — 100% misalignment.
- **Negatives in positive prompt: yes.** Tokens: `blank jersey`,
  `unmarked book covers`, `unlabeled bottles`, `no signage`,
  `no banners`, `no watermark`, `no logo`, `no caption`, `no street
  signs`. The "blank jersey" / "unmarked" noun phrases are interpreted
  as SUBJECTS by Z-Image-Turbo, not negations.
- `negative_prompt` field: **empty on 77/77** panels (target: per-panel
  negatives like `"text, watermark, logo, signage"` shipped to
  Z-Image-Turbo's negative-prompt channel — currently unused).
- Style anchor token: **0/77** panels.
- Cache_hit: **0/77** panels (cache poisoning check clean — every
  panel was a fresh GPU call).
- Pattern: **F11 + F12 + F20 + memory `project_z_image_turbo_verb_led_prompts.md`**.
- Primary fix: **O8** (Z-Image-Turbo refiner rewrite) + **O37**
  (wire refiner into long-form path) + **O33** (wire `image_style_prefix`
  into the per-panel wire prompt).

### Body-capture coverage

- `llm.call`: **populated 17/17** (`input_preview` len = 4000 chars,
  truncated marker `input_truncated=true`).
- `tts.chunk`: **populated 99/99** (`input_preview` len ≥ 104 chars,
  e.g. *"But I still wonder how close we came to a version of that flight…"*).
- `image.gen` (dotted, canonical): **populated 77/77** (`input_preview`
  + `final_prompt_preview` + `final_prompt_sha256`).
- `image_gen` (underscore, legacy duplicate): empty 77/77 — but it's
  a redundant emit site, not a body-capture regression (see Finding 5).
- `asr.*`: **N/A** — ASR stage is skipped on this render path
  (captions aligned from TTS-side timings per Firestore
  `timeline[asr].msg`).
- **Verdict: body-capture is healthy across all real stages.
  F24 (Dockerfile-COPY drift) is NOT present on this render** — the
  prior shallow pass got this correct. The render-worker-v2 image
  SHA `cc0314d2…` was built with `_tel_track_io.py` correctly COPY'd
  (per fix `8345624`).

### Stage-envelope parity

- `stage.start` events: **1** (`stage=upload`).
- `stage.end` events: **1** (`stage=upload`).
- `stage.failed` events: **0**.
- **Missing closures: rewrite, cast (skipped), engine.pick, tts,
  asr (skipped), images, timeline, overlay, compose** — 7 active
  stages with no envelope.
- Pattern: F25 (rev-healthy = deploy complete gap) + [NEW-O-CANDIDATE] **O34**.

### Channel-rule compliance

- Closer match: **partial** — channel YAML's closer is AITA-shaped
  (`"LIKE if YTA, COMMENT if NTA. AITA?"`); not applied to r/nosleep
  long-form. Narration ends with a natural meta-reflection. No
  per-niche closer config.
- Banned phrases detected: none (didn't run a banned-phrase grep, but
  the narration reads as in-band for r/nosleep tone).
- Duration vs channel band: `1612s` within `long_form.duration_target_s:
  [600, 1800]` — **96.7% of upper bound, in-band.**
- WPM: ~175 effective (`tts_speed=0.95 × tts_post_atempo=0.92`) — slightly
  fast for sleep-channel target band but per-channel intentional.
- Style aesthetic: **catastrophic mismatch** — channel declares
  watercolor (top-level) and crayon (long_form override); shipped as
  photoreal. See Finding 2.

## Recommended next actions

1. **(HIGH)** Wire the `prompt_refiner` (or a new long-form-specific
   refiner) into `pipeline/render/visualize/longform_panels.py` between
   panel-scene extraction and `images_cloudrun` invocation. The refiner
   must produce verb-led, 100-200-word, positive-only, style-anchored
   prompts per memory `project_z_image_turbo_verb_led_prompts.md`.
   Targets: **O8 + O37 (new)** + memory rule 13-17 compliance.
   Effort **M**. Lifts every long-form mystoriesanimated render.
2. **(HIGH)** Wire `spec.extra["image_style_prefix"]` end-to-end in
   the long-form path. Plugin reads it, prepends to the per-panel
   prompt before `_append_anti_text_suffix` runs. Add a regression
   test pinning that the wire prompt contains the channel's
   style_prefix substring. Targets: **O33 (new)**.
   Effort **S**. Closes Finding 2.
3. **(HIGH)** Audit + fix `images_cloudrun.py:492-520` `ANTI_TEXT_PREFIX`.
   Replace with a SHORT, positive-framed scene-tail
   (e.g. `"plain unmarked surfaces; no readable text in frame."`)
   or move to Z-Image-Turbo's `negative_prompt` channel — currently
   sent positive at char 0. Per memory rule 16 (positive-only) the
   current "blank jersey" / "unmarked book covers" noun phrases
   actively poison the prompt. Targets: **O8** + memory rule 16.
   Effort **S** (~1 hour). Closes Finding 1.
4. **(HIGH)** Fix `pipeline/render/overlays/word_caption_pngs.py` to
   actually emit per-word PNGs (not per-section blocks). 1 PNG per 146s
   of audio is not a "word caption" — see Finding 3. Targets: **F22 +
   O13 + O28**. Effort **M**. Should be paired with a post-render
   caption-density gate (O28) that hard-fails when caption-PNG count
   per second of audio < 0.5 (i.e. fewer than 1 PNG per 2s of audio).
5. **(MEDIUM)** Wire `image_steps` from channel YAML into `spec.extra`
   via `spec_enrich.populate_render_extras()`. Today the long-form
   path falls back to `steps=4` because neither the top-level YAML
   nor the `long_form:` override is read. Targets: **O25 + O26**.
   Effort **S**. Lifts every long-form mystoriesanimated render.
6. **(MEDIUM)** Audit `cloud/render-worker-v2/Dockerfile` for music
   bed assets. `music_bed_default: ambient_low.mp3` doesn't resolve
   inside the worker container → silent fallback for every long-form
   render. Either COPY assets into the image OR pull from GCS at
   warm-up. Hard-fail (not silent) when a configured bed is missing.
   Targets: **O30 + O36 (new) + F24 sibling pattern**. Effort **S**.
7. **(MEDIUM)** Extend stage-envelope wrapping to all 8 pipeline
   stages (rewrite, cast, engine.pick, tts, asr, images, timeline,
   compose) on render-worker-v2 — today only `upload` wraps.
   Add `tests/observability/test_stage_envelope_coverage.py` enumerating
   the stages that MUST emit envelopes for each render-kind.
   Targets: **F25 + O30 + O34 (new)**. Effort **M**.
8. **(MEDIUM)** Restore decision_log writes. At minimum: `engine.pick`,
   `music.pick`, `image.gen.retry` should land in Firestore
   `decision_log[]` AND the GCS `decision_log/` subdir. Targets:
   **O38 (new)**. Effort **S**. Rename misleading `envelope/`
   subdir or stop double-writing the script artifact.
9. **(LOW)** Delete the legacy `image_gen` (underscore) emit site;
   consolidate on `image.gen`. Targets: **O35 (new)**. Effort **S**.
10. **(LOW)** Flip `reasoning_effort` to `medium` for
    `rewrite_long_form_outline` to avoid the `length`-finish retry.
    Targets: **O4**. Effort **S** (one constant). Already IN-FLIGHT
    in W1 per `/ai/improvement-opportunities.md` line 24 status block.

## Self-learning notes

- **New fragility candidates (3):**
  - **F27** — channel `image_style_prefix` lossy through engine →
    visualize plugin → images_cloudrun call (long-form path).
  - **F28** — duplicate `image.gen` + `image_gen` emit sites (parallel-naming
    drift across telemetry surfaces).
  - **F29** — long-form image path has NO refiner stage; outline-LLM
    authors panel-scene strings directly, bypassing
    `pipeline/images/prompt_refiner.py`. O8 alone does NOT lift
    long-form.
- **New O-candidates (6):**
  - **O33** — wire `image_style_prefix` end-to-end (closes F27).
  - **O34** — bring stage-envelope coverage to all 8 pipeline stages
    on render-worker-v2 (closes the gap behind Finding 4).
  - **O35** — delete legacy `image_gen` emit site (closes F28).
  - **O36** — audit + COPY music_bed assets (or pull from GCS) +
    hard-fail when a configured bed is missing (closes Finding 6).
  - **O37** — wire `prompt_refiner` into the long-form image path
    (closes F29; load-bearing fix for Finding 1).
  - **O38** — restore decision_log writes for `engine.pick`,
    `music.pick`, `image.gen.retry`; stop the empty `decision_log/`
    artifact + the misleading `envelope/` duplicate (closes
    Finding 12).
- **Patterns NOT previously seen in this skill's learnings:**
  - "Two stacked anti-text safety nets at different layers" — the
    outline-LLM is instructed to suffix `"AVOID all readable text"`
    to panel scenes that feature screens/signs (only 2/77 panels
    received this), AND `images_cloudrun.py` separately prepends a
    190-char `ANTI_TEXT_PREFIX` to 77/77 panels. The two layers
    don't know about each other; the prepended layer is the
    dominant token-budget consumer.
  - "channel YAML declares two `image_*` configurations
    (top-level + `long_form:` block) and neither reaches the renderer"
    — a wiring bug that affects the top-level YAML AND the long_form
    override symmetrically. Both block-style and channel-level
    `image_steps: 9` are ignored; render uses code-default 4.
  - "envelope/ artifact subdir is a stale double-write of script.json"
    — the GCS layout has both `script/<slug>.json` and
    `envelope/<slug>.envelope.json` carrying the same payload.
    Misleads the operator (and this skill) into looking for
    stage-envelope JSONs there.

### Corrections to the previous shallow pass

- **Finding #1** — Previous draft said "~58% of rendered panels
  collapse to floating white t-shirts." Actual visual inspection
  shows panel_000 + panel_038 are PHOTOREAL women in airplane cabins
  (Findings 1 + 2 combined); panel_076 IS a floating white t-shirt
  with no human, no scene. **75% of panels (58/77) have no human
  subject at all** in the prompt — those are the panels that resolve
  most strongly to product-photo mode. The previous "58%" figure was
  the mean boilerplate / total_chars ratio (0.626 = 62.6%), not the
  panel failure rate. Corrected: mean boilerplate ratio 56.3% across
  all 77 panels, max 81.8% on single panel.
- **Finding #2** — Previous draft framed the lack of style anchor as
  "4+ incompatible visual styles." Actual evidence shows the renderer
  applied **NO** style anchor at all (0/77 panels carry any style
  token); Z-Image-Turbo's default mode produced photoreal output on
  panels with a human subject and floating-product-photo mode on
  panels without. There are not "4+ styles" — there are 2 default
  failure modes (photoreal + product-photo) and 0 of the channel's
  configured aesthetics.
- **Finding #4** — Previous draft said "only `upload` wraps." Confirmed
  correct; sharpened the recommendation by adding **O34** (new) as
  the fix target.
- **Finding #5** (refiner emits no events) — Previous draft framed as
  a missing emitter; actually the long-form path **has no refiner
  stage at all** (no prompts_refined / refiner_io artifacts). F29
  (new) is the right tag, not "the refiner emits no events."
- **Finding #6** — Previous draft said "duplicate `image.gen` +
  `image_gen` emit (77 of each)." Confirmed correct; sharpened by
  pointing to body-capture asymmetry (`image.gen` carries
  input_preview, `image_gen` does not) and proposing **O35** (delete
  the underscore site).
- **Finding #7 (F24 NOT present)** — Confirmed correct. Body capture
  is healthy 17/17 + 99/99 + 77/77 across llm/tts/image. Render-worker
  image SHA `cc0314d2…` ships `_tel_track_io.py` correctly per fix
  `8345624`.
- **Finding #8 (silent music bed)** — Confirmed correct; sharpened
  by adding **O36** (new) as the structural fix vs the prior pass's
  generic flag.
