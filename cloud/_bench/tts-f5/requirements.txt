# ytFactory TTS Cloud Run service requirements
# Pinned to versions known to work together on CUDA 12.4 (Cloud Run L4).
# IMPORTANT: torch is installed in the Dockerfile via the cu124 wheel index;
# do NOT list it here or pip will pull the CPU build by default.

fastapi==0.116.0
uvicorn[standard]==0.32.1
pydantic==2.9.2
soundfile==0.12.1
numpy==1.26.4

# F5-TTS upstream PyTorch implementation. NOT the MLX fork.
# Pinned to current latest known-good version (PyPI as of 2026-05-05).
# Upgrades require a smoke test against the local MLX output to confirm
# voice character is unchanged — small lib bumps have changed defaults.
f5-tts==1.1.20

# Helper used by the Dockerfile pre-download step + by f5_tts internally
# for HF asset pinning. 1.8.10+ is required because 1.6.x pinned
# huggingface-hub<0.24 which conflicts with gradio's >=0.33.5 (gradio
# is a transitive dep of f5-tts via its built-in web demo).
cached-path==1.8.10

# Pin huggingface-hub up-front so the resolver doesn't backtrack
# through 15+ gradio versions trying to satisfy old cached-path.
huggingface-hub>=0.33.5,<2.0

# GCS for >5MB WAV handoff (long-form chunks). Cloud Run service account
# auto-authenticates via metadata server; no creds file needed.
google-cloud-storage==2.18.2

# Phase 3 (lazy-imported in models/* — keep deps off the critical path):
#   chatterbox-tts, boson-higgs-audio (TBD pkg name), cosyvoice
# Don't add until Phase 3 deploy or the first cold-start balloons.
