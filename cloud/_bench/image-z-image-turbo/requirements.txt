# ytFactory Image — Z-Image-Turbo 6B Cloud Run GPU service requirements.
# Diffusers 0.38.0 ships ZImagePipeline. transformers 4.55.0 to match
# the FLUX.2 klein service's pin (and avoid the older 4.46 issue with
# diffusers/transformers handshake). Validated via dep-probe Cloud
# Build 2026-05-07.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.9.2
pillow==10.4.0
google-cloud-storage==2.18.2
huggingface-hub>=0.25.0,<2.0
safetensors>=0.4.0
transformers==4.55.0
accelerate==0.34.2
sentencepiece==0.2.0
einops>=0.7.0
ftfy>=6.0
diffusers==0.38.0
