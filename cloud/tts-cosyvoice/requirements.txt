# ytFactory TTS-CosyVoice Cloud Run service requirements
# torch is installed in the Dockerfile via the cu121 wheel index;
# do NOT list it here.
#
# Anything CosyVoice itself pins lives in /opt/CosyVoice/requirements.txt
# and is installed AFTER this file (so our pins win on overlap).

fastapi==0.116.0
uvicorn[standard]==0.32.1
pydantic==2.9.2
soundfile==0.12.1
numpy==1.26.4

# Helper for HF asset pinning (CosyVoice weights pre-pulled in Dockerfile).
huggingface-hub>=0.33.5,<2.0

# GCS for >5MB WAV handoff (long-form chunks). Cloud Run service account
# auto-authenticates via metadata server.
google-cloud-storage==2.18.2

# ---- OTEL_DEPS (managed by cloud/_shared/append_otel_deps.sh)
# OTel dependencies appended to every cloud/*/requirements.txt by
# cloud/_shared/append_otel_deps.sh.
#
# Pinned to >=1.27 / 0.48b0 to match the laptop venv (see top-level
# requirements.txt). The cloud-monitoring exporter is alpha upstream
# (1.12.0a0) — installs cleanly with --pre but downgrade-pin allowed.
#
# Required by cloud/_shared/otel_init.py.
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-gcp-trace>=1.7.0
opentelemetry-exporter-gcp-monitoring>=1.7.0a0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-requests>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-resourcedetector-gcp>=1.7.0a0
opentelemetry-propagator-gcp>=1.7.0
