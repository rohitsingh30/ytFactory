# ytFactory TTS — IndicF5 service requirements
# Pinned to versions compatible with CUDA 12.4 (Cloud Run L4) + the
# AI4Bharat IndicF5 fork (github.com/ai4bharat/IndicF5).
#
# IMPORTANT: torch is installed in the Dockerfile via cu124 wheel index;
# do NOT list it here or pip will pull the CPU build by default.
#
# IndicF5 fork is installed in Dockerfile via `pip install --no-deps
# git+https://github.com/ai4bharat/IndicF5.git`. Its dependencies are
# pinned here so we control them (--no-deps in the install above).

# Server
fastapi==0.116.0
uvicorn[standard]==0.32.1
pydantic==2.9.2
soundfile==0.12.1

# IndicF5 fork hard-pins (from their requirements.txt)
numpy==1.26.4              # IndicF5: numpy<=1.26.4
transformers==4.49.0       # IndicF5: transformers<4.50; their config saved at 4.49.0

# IndicF5 fork inference deps
accelerate>=0.33.0
cached_path==1.8.10        # matches tts-f5
click
datasets
ema_pytorch>=0.5.2
hydra-core>=1.3.0
jieba
librosa
pydub                       # silence-trim post-processing in their model.py
pypinyin                    # IndicF5's f5_tts/model/utils.py imports lazy_pinyin
                            # for Chinese text normalisation (also called
                            # unconditionally on Hindi text — pypinyin no-ops
                            # for non-CJK). Build-time IMPORT_SMOKE_OK gate
                            # caught this on v3 (2026-05-06).
safetensors
tomli
torchdiffeq
tqdm>=4.65.0
transformers_stream_generator
vocos                       # vocoder used by IndicF5
x_transformers>=1.31.14
matplotlib                   # IndicF5's f5_tts/infer/utils_infer.py imports
                             # at module level (likely for debug plots; we don't
                             # use them but the import is unconditional)
wandb                        # IndicF5's f5_tts/model/__init__.py re-exports
                             # Trainer, which unconditionally imports wandb.
                             # We never call Trainer but the package import
                             # chain triggers it. Build-time IMPORT_SMOKE_OK
                             # gate caught this on v4 (2026-05-06).

# HF Hub for gated download (IndicF5 is gated=auto)
huggingface-hub>=0.33.5,<2.0

# GCS for >5MB WAV handoff
google-cloud-storage==2.18.2
