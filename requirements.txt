kokoro-onnx>=0.4.0
mlx-whisper>=0.4.0
diffusers>=0.30.0
torch>=2.4.0
torchaudio>=2.4.0       # required by tts_provider: chatterbox (Resemble AI voice clone)
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.10.0          # required by diffusers for SDXL-Lightning LoRA loading
Pillow>=10.0.0
numpy>=2.0.0
soundfile>=0.12.0
huggingface_hub>=0.24.0

# Source adapters (stages 1-3)
requests>=2.31
youtube-transcript-api>=1.0
yt-dlp>=2024.10.0   # optional: only needed when YouTube captions absent + you want whisper fallback

# Stage 8 — YouTube upload (pipeline/upload.py)
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Cross-channel engagement (pipeline/cross_engage.py) — anonymous muted
# tab plays each new upload to register a natural view. After install
# run `playwright install chromium` once.
playwright>=1.45

# Control plane + laptop agent (cloud migration)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.10
httpx>=0.28
psutil>=6.0
google-cloud-firestore>=2.27
google-cloud-storage>=3.10
google-cloud-bigquery>=3.25
openai>=1.50  # Azure OpenAI client used by control/chat_service.py

# Optional — opt-in providers (install only when switching the
# corresponding channel YAML knob)
# parakeet-mlx>=0.2     # asr_provider: parakeet_mlx        (~600 MB on first use)
# chatterbox-tts>=0.1   # tts_provider: chatterbox          (~3 GB on first use; install with --no-deps to avoid downgrading transformers)
# styletts2 — wired in pipeline/audio.py but PyPI 0.1.6 has hard pins (huggingface_hub<0.20, librosa<0.11, networkx<3) that conflict with this venv. Skip in this repo; long-form sleep uses Kokoro instead.
